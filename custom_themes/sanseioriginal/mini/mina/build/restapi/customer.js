// Generated by CoffeeScript 2.0.2
(function() {
  var cancel_pending_order, check_cart, complete_order, del_customer_bargain, get_member, get_order, join_member, list_orders, login, open_customer_bargain, purchase_order, requests, update_member;

  requests = require('../libs/requests.js');

  login = function(data) {
    return requests.post('/customer/login', data);
  };

  join_member = function(data) {
    return requests.post('/customer/member', data);
  };

  get_member = function(args) {
    return requests.get('/customer/member', args);
  };

  update_member = function(data) {
    return requests.put('/customer/member', data);
  };

  check_cart = function(data) {
    return requests.post('/customer/cart', data);
  };

  purchase_order = function(data) {
    return requests.post('/customer/purchase', data);
  };

  open_customer_bargain = function(data) {
    return requests.post('/customer/bargain', data);
  };

  del_customer_bargain = function(data) {
    return requests.remove('/customer/bargain');
  };

  get_order = function(order_id, args) {
    return requests.get('/customer/order/' + order_id, args);
  };

  complete_order = function(order_id, data) {
    return requests.put('/customer/order/' + order_id, data);
  };

  cancel_pending_order = function(order_id, args) {
    return requests.remove('/customer/order/' + order_id, args);
  };

  list_orders = function(args) {
    return requests.get('/customer/order', args);
  };

  module.exports = {
    login: login,
    member: {
      get: get_member,
      create: join_member,
      update: update_member
    },
    check_cart: check_cart,
    purchase: purchase_order,
    bargain: {
      open: open_customer_bargain,
      remove: del_customer_bargain
    },
    order: {
      list: list_orders,
      get: get_order,
      done: complete_order,
      cancel: cancel_pending_order
    }
  };

}).call(this);
